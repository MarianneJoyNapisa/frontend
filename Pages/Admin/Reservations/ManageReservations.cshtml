@page
@model HomeownersMS.Pages.Admin.Reservations.ManageReservationsModel
@{
    ViewData["Title"] = "Manage Reservations";
    ViewData["HeaderTitle"] = "Manage Reservations"; 
    ViewData["CurrentPage"] = "/Admin/Reservations/ManageReservations";
}

<div class="container">
    <h2>Facility Reservation Requests</h2>
    
    @if (Model.Reservations.Any())
    {
        <div class="table-responsive">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th>Facility</th>
                        <th>Resident</th>
                        <th>Date</th>
                        <th>Time</th>
                        <th>Status</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var reservation in Model.Reservations)
                    {
                        <tr>
                            <td>@reservation.Facility.Name</td>
                            <td>@reservation.FullName</td>
                            <td>@reservation.ReservationDate.ToShortDateString()</td>
                            <td>@reservation.StartTime - @reservation.EndTime</td>
                            <td>
                                <span class="badge 
                                    @(reservation.Status == RequestStatus.Approved ? "bg-success" :
                                      reservation.Status == RequestStatus.Declined ? "bg-danger" : "bg-warning")">
                                    @reservation.Status
                                </span>
                            </td>
                            <td>
                                @if (reservation.Status == RequestStatus.Pending)
                                {
                                    <form method="post" asp-page-handler="Approve" class="d-inline">
                                        <input type="hidden" name="id" value="@reservation.FacilityRequestId" />
                                        <button type="submit" class="btn btn-sm btn-success">Approve</button>
                                    </form>
                                    <button type="button" class="btn btn-sm btn-danger" 
                                            data-bs-toggle="modal" 
                                            data-bs-target="#declineModal"
                                            data-id="@reservation.FacilityRequestId">
                                        Decline
                                    </button>
                                }
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="alert alert-info">
            No reservation requests found.
        </div>
    }
</div>

<!-- Decline Modal -->
<div class="modal fade" id="declineModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title">Decline Reservation</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <form method="post" asp-page-handler="Decline">
                <div class="modal-body">
                    <input type="hidden" name="id" id="declineReservationId" />
                    <div class="form-group">
                        <label for="adminNotes">Reason for declining:</label>
                        <textarea class="form-control" name="adminNotes" rows="3"></textarea>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-danger">Confirm Decline</button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $('#declineModal').on('show.bs.modal', function (event) {
            var button = $(event.relatedTarget);
            var id = button.data('id');
            $('#declineReservationId').val(id);
        });
    </script>
}