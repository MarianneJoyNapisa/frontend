@page
@model HomeownersMS.Pages_Admin_Admins.EditModel

@{
    Layout = "_AdminLayout";
    ViewData["HeaderTitle"] = "User Management";
    ViewData["Title"] = "Edit Admin";
}
<style>
    .card-header {
        background-color: var(--cta-color);
    }
</style>
<div class="container py-4">
    <div class="card shadow-sm rounded-3">
        <div class="card-header d-flex justify-content-between align-items-center">
            <h5 class="mb-0 text-white">Edit Admin</h5>
            <div>
                <a class="btn btn-sm btn-outline-light" asp-page="./Index">
                    <i class="bi bi-arrow-left"></i> Back to List
                </a>
            </div>
        </div>

        <div class="card-body">
            <div class="row">
                <div class="col-md-3 text-center mb-4">
                    <img src="@(string.IsNullOrEmpty(Model.Admin.ProfileImage) 
                        ? Url.Content("~/images/default-profile.png") 
                        : Url.Content($"~/uploads/{Model.Admin.ProfileImage}"))"
                        alt="Profile Image"
                        class="rounded-circle shadow mb-3"
                        style="width: 150px; height: 150px; object-fit: cover; background-color: #e9ecef;">
                    <div class="d-grid">
                        <button type="button" class="btn btn-sm btn-secondary">
                            <i class="bi bi-camera"></i> Change Photo
                        </button>
                    </div>
                </div>
                
                <div class="col-md-9">
                    <form method="post" enctype="multipart/form-data">
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>
                        <input type="hidden" asp-for="Admin.UserId" />
                        
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="Admin.FName" class="form-label"></label>
                                    <input asp-for="Admin.FName" class="form-control" />
                                    <span asp-validation-for="Admin.FName" class="text-danger small"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="form-group mb-3">
                                    <label asp-for="Admin.LName" class="form-label"></label>
                                    <input asp-for="Admin.LName" class="form-control" />
                                    <span asp-validation-for="Admin.LName" class="text-danger small"></span>
                                </div>
                            </div>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Admin.Email" class="form-label"></label>
                            <input asp-for="Admin.Email" class="form-control" />
                            <span asp-validation-for="Admin.Email" class="text-danger small"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Admin.ContactNo" class="form-label"></label>
                            <input asp-for="Admin.ContactNo" class="form-control" />
                            <span asp-validation-for="Admin.ContactNo" class="text-danger small"></span>
                        </div>

                        <div class="form-group mb-3">
                            <label asp-for="Admin.Job" class="form-label"></label>
                            <input asp-for="Admin.Job" class="form-control" />
                            <span asp-validation-for="Admin.Job" class="text-danger small"></span>
                        </div>

                        <div class="form-group mb-4">
                            <label asp-for="Admin.HireDate" class="form-label"></label>
                            <input asp-for="Admin.HireDate" type="date" class="form-control" />
                            <span asp-validation-for="Admin.HireDate" class="text-danger small"></span>
                        </div>

                        <div class="form-group d-flex justify-content-end gap-2">
                            <a asp-page="./Details" asp-route-id="@Model.Admin.UserId" class="btn btn-secondary">
                                <i class="bi bi-x-circle"></i> Cancel
                            </a>
                            <button type="submit" class="btn btn-primary px-4">
                                <i class="bi bi-save"></i> Save Changes
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}